{
  "name": "heroku-react-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:client": "cd client && npm build",
    "start:server": "cd server && npm start",
    "install:packages": "cd server && npm install && cd .. && cd client && npm install",
    "start": "node ./server",
    "heroku-postbuild": "cd client && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {}
}
